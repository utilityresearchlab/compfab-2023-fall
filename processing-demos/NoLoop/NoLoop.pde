

void setup() {
  // Setup code - gets called once at 
  // the start of the app
    println("Setup called!");
    
  // Call noLoop() to prevent looping
  // and re-drawing
  noLoop();
}


void draw() {
  // Called ONCE when using noLoop()
  println("draw called!");
}
